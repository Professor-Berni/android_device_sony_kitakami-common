# per_proxy service
type per_proxy, domain;
type per_proxy_exec, exec_type, file_type;

# Started by init
init_daemon_domain(per_proxy)

allow per_proxy sysfs:dir r_dir_perms;
allow per_proxy sysfs:file r_file_perms;
