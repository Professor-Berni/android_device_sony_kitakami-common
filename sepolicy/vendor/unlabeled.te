allow unlabeled ashmem_libcutils_device:chr_file rw_file_perms;
allow unlabeled audioserver:fd use;
allow unlabeled bootanim:fd use;
allow unlabeled fuse_device:chr_file rw_file_perms;
allow unlabeled hal_audio_default:fd use;
allow unlabeled hal_graphics_allocator_default:fd use;
allow unlabeled hal_graphics_composer_default:fd use;
allow unlabeled radio:fd use;
allow unlabeled self:binder { call transfer };
allow unlabeled surfaceflinger:fd use;
allow unlabeled surfaceflinger:unix_stream_socket { read write };
allow unlabeled system_app:fd use;
allow unlabeled system_server:fd use;
allow unlabeled system_server:unix_stream_socket { read write };
allow unlabeled vold:fd use;
