# ril service
type hal_ril_default, domain;
type hal_ril_default_exec, exec_type, vendor_file_type, file_type;

# Started by init
init_daemon_domain(hal_ril_default)

hwbinder_use(hal_ril_default)
vndbinder_use(hal_ril_default)
